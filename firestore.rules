rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read access for articles, FAQ, and program content
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
